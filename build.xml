<?xml version="1.0"?>
<project default="test" name="lcov">
  <if>
    <os family="windows"/>
    <then><property name="composer" value="C:\Program Files\PHP\share\composer.phar"/></then>
    <else><property name="composer" value="/usr/local/bin/composer"/></else>
  </if>

  <target name="clean" description="Delete the generated files">
    <delete dir="build" quiet="true"/>
    <delete dir="doc/api" quiet="true"/>
    <delete dir="web" quiet="true"/>
    <delete includeEmptyDirs="true">
      <fileSet dir="var" includes="**/*" excludes=".gitkeep"/>
    </delete>
  </target>

  <target name="coverage" description="Upload the code coverage">
    <exec executable="vendor/bin/coveralls" passthru="true">
      <arg file="var/coverage.xml"/>
    </exec>
  </target>

  <target name="doc" description="Build the documentation">
    <exec executable="vendor/bin/phpdoc" passthru="true"/>
    <exec command="mkdocs build" passthru="true"/>
  </target>

  <target name="lint" description="Perform the static analysis">
    <phpLint>
      <fileSet dir="example" includes="*.php"/>
      <fileSet dir="lib" includes="**/*.php"/>
      <fileSet dir="test" includes="**/*.php"/>
    </phpLint>
  </target>

  <target name="test" description="Run the tests">
    <exec executable="vendor/bin/phpunit" passthru="true"/>
  </target>

  <target name="upgrade" description="Upgrade the project">
    <exec command="git reset --hard" passthru="true"/>
    <exec command="git fetch --all --prune" passthru="true"/>
    <exec command="git pull --rebase" passthru="true"/>
    <composer command="update" composer="${composer}"/>
  </target>
</project>
